**/node_modules
**/dist
**/.DS_Store
.devcontainer/dev.env
integration-tests/.auth
integration-tests/results

# Jest
coverage/
jest-junit.xml

# Yarn v4 (Berry)
.yarn/*
install-state.gz
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Live-cluster fixture for model.live.spec.ts (regenerate with ./capture-fixture.sh)
fixtures/live/

# Generated by scripts/set-ocp-target.sh from the per-target lockfiles (yarn.lock.<ocp-minor>),
# which ARE tracked. One lockfile cannot pin two OpenShift targets, and committing a single one
# would silently leave the other unpinned.
/yarn.lock
